# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — keep real secrets out of git, but DO track the template.
.env
.env.local
.env.*.local
.env.development
.env.production
.env.vercel-reference.txt
# (intentionally NOT ignoring .env.example — it's the safe template users copy)

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/
# Local database (contains user data — don't commit)
/db/cloudkv.json
/db/custom.db
/db/*.json
/db/*.db

# Verification screenshots
verify-*.png
mobile-*.png
desktop-*.png

# Tool results / scratch
/tool-results/
/dl1b.txt
/download/
/upload/
/db/custom.db
.zscripts/dev.pid

# Internal dev narrative — contains real test API keys, bot IDs, and scratch
# credentials. NEVER commit; clone users should never see this.
worklog.md
agent-ctx/
plan.md
